package eu.girc.launcher.ui;

import eu.girc.launcher.LauncherScene;
import eu.girc.launcher.SceneManager;

/**
 * Base interface identifying a Controller.
 * Used by the SceneManager for convenience.
 */
public interface IController {
//    LauncherScene getLauncherScene();

    /**
     * Sets the scene manager managing application scenes.
     * @param manager the SceneManager.
     */
//    void setSceneManager(SceneManager manager);
}
